# Aither — AI Health Scanner (Baseline)
Locked baseline (Light CSS Matrix + Self-harm Detection).  
No code edits to the scanner.

## How to run locally
pip install -r requirements.txt  
python aither_baseline.py
